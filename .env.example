# Ativa modo debug (1=ativo, 0=inativo)
DEBUG=1

# Chave secreta para JWT e segurança da aplicação
SECRET_KEY=your-secret-key-here

# URL do Redis para cache
REDIS_URL=redis://localhost:6379/7

# Celery - fila de tarefas assíncronas
CELERY_BROKER_URL=amqp://rabbit:password@localhost:5672
CELERY_BACKEND_URL=redis://localhost:6379/0

# Ambiente da aplicação: development, production ou test
ENVIRONMENT=development

# Configuração do SQL Server
DB_SERVER=localhost

# Nome do banco de dados de produção
DB_NAME_PRODUCTION=fastapi_production_db

# Nome do banco de dados de teste
DB_NAME_TEST=fastapi_test_db

# Driver ODBC do SQL Server (atenção ao nome exato instalado no sistema)
DB_DRIVER=ODBC Driver 17 for SQL Server
# Configura��o do usu�rio padr�o (para script create-default-user)
DEFAULT_USER_USERNAME=admin
DEFAULT_USER_EMAIL=admin@admin.com
DEFAULT_USER_PASSWORD=admin123
DEFAULT_USER_IS_ADMIN=true
